const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],

	theme: {
		extend: {
			fontFamily: {
				mono: ['Minecraft'],
				minecrafter: ['Minecrafter']
			}
		}
	},

	plugins: []
};

module.exports = config;
